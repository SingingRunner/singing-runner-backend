export enum GameRoomStatus {
  IN_GAME = "inGame",
  MATCHING = "matching",
}

export enum UserMatchTier {
  BRONZE = 1000,
  SILVER = 2000,
  GOLD = 3000,
  PLATINUM = 4000,
  DIAMOND = 5000,
}

export enum GameMode {
  RANK = 0,
  ITEM = 1,
  NORMAL = 2,
}
